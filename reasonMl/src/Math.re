42 + 5 mod 5 * 3;
5 mod 2;
23 mod 4;
1 + 6 * 5;
45 + (-34) / 45 * 3;

1 < 2 && 2 !== 3;
1 < 2 || 2 !== 3;

1 < 2 === true;

let f = (x: int, y: int) => x * x * y;
f(2, 4);

let isEven = x =>
  if (x mod 2 === 0) {
    true;
  } else {
    false;
  };
isEven(7);

let a = "a";
let c = "c";
a < c;
/* true */